{
  "name": "legacy-contract",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "description": "Ironwood note inheritance policy (EVM). Does not custody ZEC.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.18",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
